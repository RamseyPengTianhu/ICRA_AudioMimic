Use case: scientific-educational. Create a fresh design for a compact ForeDance robotics-paper detail figure, landscape about 3:1 on pure white. It must communicate the comparison at a glance through flat geometric imagery, with very little text. Absolutely two-dimensional, orthographic, solid colors, no perspective, extruded blocks, shaded spheres, gradients, drop shadows, glow, or 3D. Elegant IEEE/ICRA paper artwork. Helvetica-like lettering. Palette: vivid blue #0769F9 for discrete structure, warm orange #FF8A17 for continuous detail, purple #8534DB for boundary/context, teal #009EAC for recorded target, dark navy text.

Main composition: LEFT two thirds is a DIRECT VISUAL COMPARISON of Teacher Forcing and Commit Forcing. Two perfectly aligned horizontal rows, same length and visual grammar, top "Teacher Forcing", bottom "Commit Forcing". These are alternatives being compared, NOT branches feeding a shared workflow. Do NOT draw a selection switch, decision diamond, learner triangle, neural-network box, merge junction, loss nodes, or connecting arrows between the two rows.
Put short shared column headings "History" and "Boundary state" above these two rows. History is a clean flat time ribbon: blue square structure codes above an orange detail strip, with an older portion on the left and the most recent committed segment at the right. A short ellipsis separates distant past from the recent segment. Use tasteful small variations within the colored cells as abstract code content; no numbers or variable names. Next to each history, separated by a thin purple vertical divider, place a small purple, flat, anatomically consistent humanoid-pose silhouette and a short purple velocity trace. This means boundary state; no arrow attached to the body.
A delicate lavender grouping band encloses ONLY the recent history tail AND the boundary-state glyph in each row. This paired region is the key visual relationship. In the top row the two are taken from the same recorded segment; label the grouped region "Recorded segment". In the bottom row the recent code content and pose differ subtly from the top row, but retain the same anatomy and icon style, showing a model-generated commitment; label the grouped region "Generated commit". The old history stays visually the same across the rows. A single bracket per grouped region is enough. No looping arrows or explanatory sentences. Leave clear space between the top label and lower row. Rows must read as a clean, intuitive comparison rather than an algorithm chart.

RIGHT one third is an independent GEOMETRIC inset, separated only by whitespace or a very light vertical rule. It illustrates residual rebasing used with sampled structure, not another TF/CoF branch. Draw one teal target dot toward the upper right, short label "Same recorded target". Two blue structure anchor dots lie lower down: sampled structure lower-left and recorded structure lower-right. Label them simply "Sampled structure" and "Recorded structure". A strong orange vector starts at sampled structure and ends precisely at the teal target. A lighter dashed orange vector starts at recorded structure and ends precisely at the SAME teal target. Label the strong vector "Adjusted detail", the faint vector "Detail". The common endpoint, changed origin, and changed residual should explain the concept without a formula. No axes, numeric ticks, variable symbols, extra targets, loss circles, or arrow from a boundary state to the target. No connectors between the comparison rows and this independent inset.

No title banner, legend card, paragraph, footer explanation, equations, gradients notation, tiny technical labels, or panel borders. Make shapes and relationships do the explanatory work. Keep text sparse, natural proportions and legible at double-column paper width. Beautiful but restrained, with varied visual rhythms from time ribbons, posture glyphs and the residual geometry.
