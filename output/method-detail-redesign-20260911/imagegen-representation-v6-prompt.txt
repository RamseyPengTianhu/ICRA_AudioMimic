Use case: scientific-educational.
Create a NEW compact scientific illustration for a ForeDance robotics paper explaining representation learning through geometry and motion imagery. Wide panoramic 3:1 canvas, pure white. Strictly flat two-dimensional vector-like artwork: no 3D, perspective, tilted planes, shadows, glow, gradients, extrusion, spheres or texture. Helvetica-like labels. Solid blue #0769F9 for structure, orange #FF8A17 for detail, purple #8534DB for boundary state, pale blue regions and dark navy text. No title banner or outer panel box.

Tell one visual story: motion is encoded into a discrete structural anchor plus a continuous detail offset; full and structure-only reconstructions train the SAME boundary-conditioned decoder under different supervision. This is a conceptual illustration, not an inventory of equations.

LEFT approximately 20%: three small consistent, flat articulated humanoid motion silhouettes, dark navy, different dance poses but identical anatomy/proportions. Label "Input motion". A short arrow enters a small simple flat funnel labeled "Encode", with a small crisp orange flame icon to indicate learning. Its output enters the central geometry.

CENTER approximately 27%: a flat irregular pale-blue codebook region filled with a sparse lattice of solid blue prototype dots. One selected blue dot is larger, label "Structure". An orange straight arrow runs from this selected anchor to a nearby dark navy dot representing the combined latent. Label the orange arrow "Detail", and the navy dot "Full code". Label the region "Codebook". No axes, numerical ticks, variables or equations. The vector's origin and endpoint must exactly touch the intended dots. Use the geometry to make structure-plus-detail visually obvious.

CENTER-RIGHT approximately 24%: two clearly separated curved forward paths leave the central geometry. The upper dark navy path starts from Full code, label "Structure + detail"; the lower BLUE path starts from the selected Structure dot, label "Structure only". Both enter ONE tall, flat decoder shape at two different unobstructed ports. Label that ONE shared shape "Shared decoder". Add one small orange flame. Do not draw two decoder boxes, a disconnected sharing key, loss circles or gradient arrows. Between the curves, a compact purple flat pose-and-velocity glyph labeled "Boundary state" enters a third side port of this same decoder. The pose itself has no arrow on the body. All paths have generous clearance from text and each other.

RIGHT approximately 29%: two aligned rows of three flat humanoid pose silhouettes, each row reproducing the same three underlying poses with identical anatomy. One output path from the upper decoder port leads to the upper row; one from the lower port leads to the lower row. Label the upper row "Full supervision" and the lower row "Structural supervision". Upper poses show the entire body: blue torso, hips, legs and feet, with orange arms. Lower poses show the SAME complete pose geometry: structural body parts vivid blue, arm segments pale lavender, and wrists explicitly marked with blue endpoint dots. This illustrates selected supervision on a shared reconstruction, NOT two separate anatomical motion decoders. The two rows must stay beautifully aligned and consistent, not unrelated character drawings.

The ONLY text is the quoted short labels above. No loss names, mathematical symbols, subscripts, gradient signs, tensor shapes, explanatory paragraphs, tables or footnotes. Small details belong in the accompanying equations, not this figure. Use sparse, readable labels, elegant curved connections, negative space and consistent line weights. Make the relationship understandable at a glance at paper width. All icons and surfaces strictly 2D.
