\section{Experiments}
\label{sec:experiments}
We assess music--motion matching, conditional adaptation, and dance quality.
The evaluation combines generation results, matched method ablations, and
reference-to-execution measurements.

\subsection{Experimental Setup}
\label{sec:experimental_setup}
We use FineDance motion retargeted to G1 with the representation in
Eq.~\eqref{eq:motion}. The quantitative cohort comprises 18 official test tracks,
three training seeds per configuration, and three sampling seeds per trained
model. All methods use the same source-supported 23\,s segment beginning at
source frame 264. A selected 60\,s rollout provides a separate qualitative example.

Training uses 6,250 updates with batch size 48. Mixed-start training draws
20\% song-origin starts and 80\% ordinary continuation windows; continuation-start
training uses ordinary windows throughout. The model and sampler follow
Section~\ref{sec:dc}; additional optimization details are in
Appendix~\ref{app:training}. Comparisons hold source clips, initialization, and
sampling settings fixed. The downstream SONIC controller is fixed.

\subsection{Evaluation Protocol}
\label{sec:evaluation_protocol}
\paragraph{Trajectory sources}
The protocol distinguishes retargeted references, generated motion, and their
executed counterparts. Generated motion concatenates committed prefixes.
All paired scores use the same soundtrack interval and preprocessing; execution
comparisons additionally retain initialization and reference-preview settings.

\paragraph{Music--motion matching}
A frozen G1 audio--motion evaluator, trained independently of the generator,
maps paired music and motion to a shared space. Following the alignment
motivation of SoulNet~\cite{li2025soulnet}, MMR-MS combines sequence-level
correspondence and temporal feature variation; lower is better.
Appendix~\ref{app:metric_construction} describes the evaluator construction
and calibration.

\paragraph{Dance quality}
Kinetic and geometric Fr\'echet distances (FID$_k$, FID$_g$) compare G1 motion
features with retargeted reference distributions. Diversity (Div$_k$, Div$_g$)
uses pairwise distances in the same feature spaces. Feature extraction,
normalization, and reference collections are fixed within each comparison.
Joint jerk, activity, and root/contact proxies complement distribution scores.

\paragraph{Rhythm and adaptation}
Beat Alignment Score (BAS) measures the proximity of motion accents to music
beats. Event coverage and onset--motion correlation complement rhythmic
alignment. Controlled music interventions test whether generated movement
responds appropriately to the specified soundtrack.
Primary music scores retain the audio timeline; lag alignment is used only
for tracking diagnostics.

\paragraph{Statistics}
We use the complete test cohort and 10,000 hierarchical bootstrap draws,
resampling training seeds and then tracks after averaging sampling repeats
within each track. FID is evaluated on feature collections rather than
per-song scores. Appendix~\ref{app:scope} details aggregation.

\subsection{Music--Motion Matching and Dance Quality}
\label{sec:matching_quality}
Table~\ref{tab:training} reports matching, distribution, diversity, and contact
measures for clean-history and full-history-corruption (FHC) training.
Under mixed-start training, FHC reduces kinetic/geometric FID from
208.599/0.482 to 162.831/0.410, with favorable directions across all training
seeds. Continuation-start changes are smaller, from 156.218/0.372 to
154.180/0.361. The effect of history regularization therefore depends on startup.

The FHC-versus-clean-history MMR-MS intervals include zero under both settings,
while mixed-start contact and jerk measures favor clean history.
The results indicate a distribution--smoothness tradeoff rather than uniform
improvement. Complete values appear in Appendix~\ref{app:fhc}.

\paragraph{Qualitative generation}
\label{sec:showcase}
Figure~\ref{fig:sequence} shows reference poses at 10, 30, and 50\,s from one
selected 60\,s rollout.
\begin{figure*}[t]
\centering
\input{figures/foredance/sequence.tex}
\caption{\textbf{A 60-second ForeDance generation.} Original frames at 10, 30, and 50\,s from a selected FineDance sequence. The images show rendered reference motion.}
\label{fig:sequence}
\end{figure*}

\paragraph{Perceptual protocol}
The planned blinded comparison separates silent-video ratings of naturalness
and expressiveness from audio-enabled music-fit ratings, with matched
presentation and randomized method order.

\subsection{Music Adaptation}
\label{sec:music_results}
\paragraph{Music-condition use}
Table~\ref{tab:music_use} compares forecast-conditioned models with separately
trained no-music controls. Mixed-start training reduces MMR-MS by 0.782
(95\% interval [0.294, 1.285]); continuation-start training reduces it by 1.123
([0.621, 1.649]). All three training seeds agree in direction under both settings.
\begin{table}[t]
\centering\small
\caption{\textbf{Music-condition use.} Reduction in MMR-MS relative to matched no-music training; positive is favorable. Paired hierarchical 95\% intervals use the complete 18-track cohort.}
\label{tab:music_use}
\begin{tabular}{@{}lcc@{}}\toprule
Startup & Improvement & 95\% interval\\\midrule
\inputtable{evidence/foredance/music_use_rows.tex}
\bottomrule\end{tabular}
\end{table}

\paragraph{Forecast versus heard music}
The matched design in Table~\ref{tab:anticipation} varies the music states while
fixing representation, conditioning capacity, CoF, startup, and training budget.
\begin{table*}[t]
\centering\small
\caption{\textbf{Forecast versus heard-music conditioning.} Matched comparison design; results are pending (---). Each metric uses identical sources and sampling conditions across methods.}
\label{tab:anticipation}
\begin{tabular}{@{}lcccccc@{}}\toprule
Condition & MMR-MS $\downarrow$ & BAS $\uparrow$ & FID$_k$ $\downarrow$ & FID$_g$ $\downarrow$ & Div$_k$ & Div$_g$\\\midrule
Heard-music states & \pendingcell & \pendingcell & \pendingcell & \pendingcell & \pendingcell & \pendingcell\\
Predicted-music states & \pendingcell & \pendingcell & \pendingcell & \pendingcell & \pendingcell & \pendingcell\\\bottomrule
\end{tabular}
\end{table*}

\paragraph{Conditional specificity}
The paired intervention protocol uses correct, exact-null, same-track wrong-time,
same-genre wrong-track, cross-genre wrong-track, and $\pm4$ music-frame shifts
($\pm160$\,ms). Initial history, boundary state, and sampling seed are matched;
each rollout then advances with its own committed history. Outputs are scored
against the designated soundtrack. These inference-time interventions differ
from separately trained no-music controls.

For songs $a,b$ and generated dances $D_a,D_b$, the correct-pair advantage is
\begin{equation}
\Delta_{\mathrm{pair}}=\tfrac12\big[
d(D_a,b)-d(D_a,a)+d(D_b,a)-d(D_b,b)\big],
\label{eq:cross_pair_advantage}
\end{equation}
where $d$ is a lower-is-better matching distance. Positive values favor intended
pairings; both directional effects are retained. The cross-pairing design applies
to generated and executed motion; its matched results remain to be filled.

\subsection{Ablation Studies}
\label{sec:ablations}
\subsubsection{D+C Representation and Training}
\label{sec:dc_results}
The representation study measures structure/detail reconstruction, branch-swap
effects, and residual effective rank. The generation comparison
(Table~\ref{tab:dc}) independently trains discrete-only, continuous-only,
and D+C alternatives under matched data and optimization settings.
\begin{table}[t]
\centering\small
\caption{\textbf{Representation and training comparison.} Matched generator results are pending (---).}
\label{tab:dc}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lcccc@{}}\toprule
Representation & FID$_k$ $\downarrow$ & FID$_g$ $\downarrow$ & Div$_k$ & Div$_g$\\\midrule
Discrete only & -- & -- & -- & --\\
Continuous only & -- & -- & -- & --\\
D+C with paired training & -- & -- & -- & --\\\bottomrule
\end{tabular}
\end{table}

\subsubsection{Commit Forcing versus Teacher Forcing}
\label{sec:cof_results}
Table~\ref{tab:cof} changes TF to CoF while fixing the codec, music conditions,
startup, history corruption, training budget, and inference sampler.
Evaluation uses 60\,s recursive rollouts and three non-overlapping 20\,s
intervals. Outcomes include matching, distribution/diversity, activity,
freezing, root drift, and boundary position/velocity discontinuities.
\begin{table}[t]
\centering\small
\caption{\textbf{Commit Forcing versus Teacher Forcing.} Matched 60\,s rollout results are pending (---).}
\label{tab:cof}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lcccc@{}}\toprule
Training & FID$_k$ $\downarrow$ & FID$_g$ $\downarrow$ & Div$_k$ & Div$_g$\\\midrule
Teacher Forcing & -- & -- & -- & --\\
Commit Forcing & -- & -- & -- & --\\\bottomrule
\end{tabular}
\end{table}

\subsection{Online Execution and Reliability}
\label{sec:execution_results}
The execution protocol compares generated, bridge-sent, controller-consumed,
and measured trajectories. Joint RMSE, amplitude and squared-velocity retention
measure tracking, while matching, rhythm, and motion-quality scores assess the
resulting dance. Executed dataset references provide a calibration control.
Supporting tracking measurements are given in Appendix~\ref{app:execution}.

Offline--online controls separately vary condition content/age and reference
delivery, retaining source-trajectory identity and preview duration. Runtime
reporting includes valid forecast coverage, planning-time tails, deadline
misses, and stale-condition fallback. Startup and steady state are reported
separately, with all attempts included in completion statistics.
